# API token for https://tools.tornevall.com
# When set, the app uses this as the default key (no manual entry needed).
# Vite exposes variables prefixed with VITE_ to the browser bundle.
VITE_API_TOKEN=your-api-token-here
